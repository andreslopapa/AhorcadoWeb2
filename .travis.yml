language: java
jdk:
  - oraclejdk8
  - openjdk8

sudo: required
addons:
  chrome: stable

before_install:
  - cd java

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start

script: mvn clean test
