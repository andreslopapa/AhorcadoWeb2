image: maven:latest

cache:
  paths:
    - .m2/repository/
    - target/


test:
  stage: test
  script:
    - mvn clean test

deploy:
  stage: deploy
  script:
    - mvn $MAVEN_CLI_OPTS deploy
  only:
    - master
